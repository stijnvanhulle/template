---
'@stijnvanhulle/template-claude-plugin': patch
'@stijnvanhulle/template-cursor-plugin': patch
---

The `pr` skill now runs the `humanizer` skill over the PR body and holds every output it writes
to what a reader has to act on, so descriptions stay short and specific.
