As a <persona>, I want <goal>, so that <benefit>.

Acceptance criteria

* <one behavior, written as what the user sees, and true or false on sight>
* <the feature flag, permission or role that gates it>
* <the empty, loading or error state>

`<METHOD> /api/<path>`

<what the endpoint returns, and what it leaves to the frontend>

```json
{ "field": "value" }
```

Out of scope

* <the part another ticket owns>, that is [KEY-123](https://<site>.atlassian.net/browse/KEY-123)
